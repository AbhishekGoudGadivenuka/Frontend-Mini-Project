import React from 'react'

function vue() {
  return (
    <div>
    <h1 className='display-3 text-secondary text-center'>Vue
    </h1>
    <p className='lead'>Node. JS is a runtime environment for executing JavaScript on the server side, while Vue. JS is a frontend JavaScript framework for building user interfaces. Node JS is used for server-side development, APIs, and networking, while Vue.</p>
</div>
  )
}

export default vue